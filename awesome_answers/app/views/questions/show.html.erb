<h1><%= @q.title %></h1>
<p><%= @q.body %></p>
<%#= link_to "Edit", edit_question_path(id: @q.id) %>
<%= link_to "Edit", edit_question_path(@q) %>
<%= button_to "Delete", question_path(@q), method: :delete,
                                           data: {confirm: "Are you sure?"}%>
